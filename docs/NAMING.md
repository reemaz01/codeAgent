# NAMING.md — CodePilot Agent → codeAgent

**Finalized project name: `codeAgent`**

## Rationale

- Matches the GitHub repository name already created on Day 1 (`reemaz01/codeAgent`) — no need to rename the repo or re-point deployed URLs later.
- Short, clear, and matches the product's core identity: an AI agent that works with code.
- The PRD's working title "CodePilot Agent" is superseded by this name. All new documentation (this Day 2 deliverable set, future READMEs, pitch deck updates) should refer to the product as **codeAgent**.

## What changes because of this

- No functional/architectural changes — this is a naming decision only.
- README.md (root) should use "codeAgent" as the project title going forward.
- Pitch deck title slide should be updated to "codeAgent" before Day 10 presentation (flagged for a later polish pass, not urgent today).
